# Daily Journal

캘린더, 할 일, 일기, 무드 트래커가 모두 담긴 하루 저널.

## 실행

```bash
npm install
npm run dev
```

터미널에 나오는 `http://localhost:5173` 주소를 브라우저에서 열면 됩니다.

## 배포용 빌드

```bash
npm run build
```

## 데이터 저장

입력하신 모든 내용은 브라우저의 localStorage에 저장됩니다.
브라우저/기기마다 따로 저장되며, 시크릿 모드에서는 저장되지 않습니다.
